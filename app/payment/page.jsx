import { Box, Container, Typography } from "@mui/material";

const BookedPage = () => {
  // TODO:Handle payment for tour booking confirmation

  return (
    <Container sx={{ my: 16 }}>
      <Typography variant="h4" textAlign={"center"}>
        Pay Now
      </Typography>
    </Container>
  );
};

export default BookedPage;
